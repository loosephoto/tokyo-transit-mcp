# Tokyo Transit MCP Server

ODPT API および 気象庁 API を利用した東京圏総合交通情報MCPサーバー

## 環境変数
- `ODPT_API_KEY`: 公共交通オープンデータセンター（https://developer.odpt.org/）から取得したAPIキー（必須）
- `FLIGHT_API_KEY`: フライト時刻取得用 AviationStack（https://aviationstack.com/）のAPIキー（任意）。未設定時は空港アクセス経路のみ表示（graceful degradation）
